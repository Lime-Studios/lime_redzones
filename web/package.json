{
  "name": "lime_redzones-ui",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "NUI for lime_redzones. Build with `npm install` then `npm run build` — outputs straight into web/bundle.js and web/bundle.css.",
  "scripts": {
    "build": "vite build",
    "watch": "vite build --watch"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "svelte": "^5.19.0",
    "vite": "^6.0.7"
  }
}
